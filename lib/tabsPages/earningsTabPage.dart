import 'package:flutter/material.dart';

class EarningTabPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Esta es la sección Ganancias"),
    );
  }
}
